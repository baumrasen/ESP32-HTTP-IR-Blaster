// src/credentials.h.example
// WICHTIG: Diese Datei in credentials.h umbenennen und die Platzhalter
//          mit den echten Zugangsdaten ersetzen!
//          Sicherstellen, dass credentials.h in .gitignore steht!

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// --- WiFi Netzwerk ---
const char* ssid = "DEIN_WLAN_SSID_HIER";         // Ersetze dies mit deinem WLAN-Namen
const char* password = "DEIN_WLAN_PASSWORT_HIER"; // Ersetze dies mit deinem WLAN-Passwort

// --- Gerätekonfiguration (Wird verwendet, da WiFiManager deaktiviert ist) ---
const char* custom_hostname = "irctrl-esp32"; // Wähle einen Hostnamen für das Gerät
const char* custom_passcode = "";             // Setze ein Passwort für API-Zugriff (leer lassen für keins)
const char* custom_port = "80";               // Webserver-Port (Standard ist 80)

#endif // CREDENTIALS_H
